============
Installation
============

Install django-model-publisher::

    pip install django-model-publisher


Add to installed apps::

    INSTALLED_APPS = (
        ...

        'publisher',
    )
